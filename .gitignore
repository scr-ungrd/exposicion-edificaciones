# Entorno virtual y dependencias de Python
venv/
__pycache__/
separador.py
requirements.txt

# Documentos originales y salidas intermedias (regenerables)
data/*
# ...excepto los scripts (reproducibilidad), el pmtiles del mapa interactivo
# (37.7 MB, cabe bajo el límite de GitHub y el capítulo lo necesita en producción)
# y el CSV de área construida por municipio (21 KB, referenciado como descarga
# desde 02-mapa-exposicion.qmd)
!data/*.py
!data/*.sh
!data/edificios.pmtiles
!data/area_por_municipio.csv

# Archivos y carpetas generadas automáticamente por Quarto
_book/
.quarto/

/.quarto/

**/*.quarto_ipynb
